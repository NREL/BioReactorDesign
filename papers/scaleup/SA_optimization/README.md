
# Optim

`python get_optimal_all_qois.py --plot-only` regenerates the optimization plots

`python get_optimal_all_qois.py` recomputes the optimization

# Constrained optim

`python get_optimal_constrained.py` redoes the constrained optimization (constrains the number of spargers)

`python plot_marginal_gain.py` plots the results of the constrained optimization

# Latex tables

`python make_hyperparam_tables.py` makes the latex tables with the optimal hyperpar

`python make_val_rmse_table.py` makes the surrogate perf tables

