`python randsim.py` generates study folders that contain an ensemble of Openfoam case folders, each with a different design variable sample

The case folders use `template` as a template openfoam folder to create the new openfoam case folders. 
